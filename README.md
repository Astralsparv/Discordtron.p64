# Discordtron.p64
A discord bot client for Picotron - requires an external python script

Run `main.py` from `/py/`, you need to replace `BOT_TOKEN_HERE` with your discord bot token (this is on the last line of `main.py`).